import type { SizeUnit } from '../types';

const space: Record<SizeUnit, string> = {
  xxs: 'var(--space-xxs)',
  xs: 'var(--space-xs)',
  s: 'var(--space-s)',
  m: 'var(--space-m)',
  l: 'var(--space-l)',
  xl: 'var(--space-xl)',
  xxl: 'var(--space-xxl)',
};

export { space };
